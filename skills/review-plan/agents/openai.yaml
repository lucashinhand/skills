interface:
  display_name: "Review Plan"
  short_description: "Grounded, verdict-first implementation plan reviews"
  default_prompt: "Use $review-plan to review the plan at the supplied file path."
policy:
  allow_implicit_invocation: true
